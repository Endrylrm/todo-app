# Todo API

A simple backend API for task management using SQLite.

This project was created for learning and educational purposes.

## Tech Stack

- Python
- SQLite
- FastAPI

## Folder Structure

```text
database/
└─── todo.db					    # Todo - SQLite Database
src/
├─── todo_api/
│    ├─── controllers/
│    │ 	  └─── todo_controller.py	# Controller - Todo API routing and controlling
│    ├─── models/
│    │ 	  └─── todo.py		        # Model - Todo Model
│    ├─── repositories/
│    │ 	  └─── todo_repository.py	# Repository - Todo Database
│    ├─── services/
│    │ 	  └─── todo_service.py		# Service - Gets Data from todo Repository
│    ├─── validations/
│    │ 	  └─── results.py	        # SQL Validation results
│    └─── main.py					# FastAPI - Entrypoint
tests/                              # tests
```

## Database Schema

```sql
CREATE TABLE todos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    is_active BOOLEAN
);
```

## Installation

### Clone the repository:

```bash
git clone <repository-url>
cd <project-name>
```

### Install dependencies:

```bash
# using pip
py -m venv .venv
pip install -r requirements.txt

# using uv
uv add -r requirements.txt
```

### Start the server:

```bash
fastapi run ./src/todo_api/main.py
```

## Endpoints

### Get all tasks

```http
GET /api/todos
```

### Get a task by ID

```http
GET /api/todos/:id
```

### Create a task

```http
POST /api/todos
```

Request body:

```json
{
  "title": "Learn SQL",
  "description": "Study SQLite fundamentals",
  "is_active": true
}
```

### Update a task

```http
PUT /api/todos/:id
PATCH /api/todos/:id
```

Request body:

```json
{
  "title": "Learn SQL",
  "description": "Study SQLite fundamentals",
  "is_active": true
}
```

### Delete a task

```http
DELETE /api/todos/:id
```

## Database

This project uses SQLite for local data storage.

The `id` field is automatically generated by the database and serves as a unique identifier for each task.

## License

Distributed under the MIT License. See `LICENSE` for more information.

Copyright (c) 2026, Endryl Richard Monteiro
